#!/usr/bin/env bash
# Install ufw and setup a few rules on a web server
apt-get -y update && apt-y install ufw
sed -i "/IPV6=no/IPV6=yes/" /etc/default/ufw
ufw default deny incoming && ufw default allow outgoing
ufw allow ssh && ufw allow http && ufw allow https
echo 'y' | sudo ufw enable
